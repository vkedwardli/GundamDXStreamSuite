# Google Cloud / YouTube API Credentials
# Get these from your Google Cloud Console (https://console.cloud.google.com/)
# Ensure the YouTube Data API v3 is enabled for your project.
# For the redirect URI, ensure "http://localhost:3000/oauth2callback" is added to your OAuth 2.0 client ID's authorized redirect URIs.
CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

# OBS WebSocket Password
# Set this in OBS Studio under Tools -> WebSocket Server Settings
OBS_PASSWORD="YOUR_OBS_WEBSOCKET_PASSWORD"

# MiniMax AI TTS (Optional - only if you plan to use MiniMax for TTS)
# Get these from your MiniMax account (https://api.minimax.chat/)
MINIMAX_GROUP_ID="YOUR_MINIMAX_GROUP_ID"
MINIMAX_API_KEY="YOUR_MINIMAX_API_KEY"

# Node Environment (Optional - defaults to production if not set)
# Set to "development" for development-specific behaviors (e.g., skipping DXOP screen launch)
# NODE_ENV="development"
